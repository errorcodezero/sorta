/**
 * Function that implements bogosort.
 * Bogo sort works by randomly shuffling the array until it is sorted.
 * @param {number[]} array - The input array to take in.
 * @returns {number[]} - The input array but sorted with bogosort
 */
export default function bogoSort(array: number[]) {
    // TODO Implement this
}
