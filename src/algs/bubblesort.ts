/**
 * Function that implements bubble sort.
 * Bubble sort works by comparing two elements next to eachother and swapping them if they aren't in the correct order, it slowly goes up the list, comparing two elements at a time and swapping them until the list is sorted.
 * @param {number[]} array - The input array to take in.
 * @returns {number[]} - The input array but sorted with bogosort
 */
export default function bubbleSort(array: number[]) {
    // TODO Implement this
}
